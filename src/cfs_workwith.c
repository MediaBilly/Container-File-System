#include <stdio.h>
#include "../headers/cfs.h"

int main(int argc, char const *argv[]) {
    // Check for correct usage
    if (argc != 2) {
        printf("Usage:cfsworkwith <FILE>\n");
    }
    return 0;
}
