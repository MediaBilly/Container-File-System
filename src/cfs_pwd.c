#include <stdio.h>
#include "../headers/cfs.h"

int main(int argc, char const *argv[]) {
    // Check for correct usage
    if (argc != 1) {
        printf("Usage:cfs_pwd\n");
        return 0;
    }
    // TODO: Implementation
    return 0;
}
